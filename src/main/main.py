from gui.tkinter.main_screen_tkinter import MainScreen

    # Press the green button in the gutter to run the script.
if __name__ == '__main__':
    # parser = GCode_Parser()
    # startup_code = parser.create_gcode()
    #
    # writer = Gcode_Writer(startup_code)
    #
    # shutdown_code: GCode = writer.modify_gcode()
    #
    #file_handler = File_Handler()

    # file_handler.write_file(shutdown_code)
    #
    #
    main_screen = MainScreen()
    main_screen.start()
